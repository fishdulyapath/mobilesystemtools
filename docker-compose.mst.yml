services:
  mobiletoolsservice:
    image: minorsoft/mobiletoolsservice:latest
    container_name: mobiletoolsservice
    restart: always
    expose:
      - "47300"
    environment:
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_IMAGES_NAME: ${DB_IMAGES_NAME}
      PORT: 47300
    networks:
      - mobiletools_net
      - sml_service_network
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:47300/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  mobiletools:
    image: minorsoft/mobiletools:latest
    container_name: mobiletools
    restart: always
    depends_on:
      mobiletoolsservice:
        condition: service_healthy
    networks:
      - mobiletools_net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  nginx:
    image: nginx:1.27-alpine
    container_name: nginx
    restart: always
    ports:
      - "8094:80"
    volumes:
      - ./nginx-mst.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - mobiletools
      - mobiletoolsservice
    networks:
      - mobiletools_net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  mobiletools_net:
    driver: bridge
  sml_service_network:
    external: true

